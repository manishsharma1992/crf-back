-- Test data for integration tests
-- This will be loaded automatically by Spring Boot for H2 database

-- No initial data needed - tests will create their own data
-- But we can verify schema is created correctly

-- Optional: Add reference data if needed